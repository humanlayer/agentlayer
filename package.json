{
  "name": "agentlayer",
  "private": true,
  "packageManager": "bun@1.3.0",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebuild": "bun install",
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^25.5.0",
    "turbo": "^2.8.1",
    "typescript": "^5.9.2"
  }
}
